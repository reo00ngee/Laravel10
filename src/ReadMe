cloneする
git clone https://github.com/reo00ngee/Laravel10 ディレクトリの名前

コンテナに入る
docker-compose exec app bash

Laravelインストール
composer create-project --prefer-dist laravel/laravel プロジェクト名

Laravelプロジェクト移動
cd プロジェクト名

ストレージの権限変更
chmod 777 -R storage/

php artisan key:generate

マイグレート

モデル、コントローラーの作成
php artisan make:model Ocr -mcr

認証機能の実装
composer require laravel/ui
php artisan ui bootstrap --auth
npm install && npm run dev
rm -rf node_modules
rm package-lock.json
npm install && npm run dev
